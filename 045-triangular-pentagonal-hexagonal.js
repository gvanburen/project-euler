/*Triangle, pentagonal, and hexagonal numbers are generated by the following formulae:

Triangle	 	Tn=n(n+1)/2	 	1, 3, 6, 10, 15, ...
Pentagonal	 	Pn=n(3n−1)/2	 	1, 5, 12, 22, 35, ...
Hexagonal	 	Hn=n(2n−1)	 	1, 6, 15, 28, 45, ...
It can be verified that T285 = P165 = H143 = 40755.

Find the next triangle number that is also pentagonal and hexagonal.*/

// brute-force - super slow
function t(n) {
    return n * (n + 1) / 2;
}

function p(n) {
    return n * (3 * n - 1) / 2;
}
var result = 0;
for (var a = 286; a <= 60000; a++) {
    for (var b = 166; b <= 35000; b++) {
        if (t(a) === p(b)) {
            result = t(a);
            console.log(result);
        }
    }
}

// optimized
function p(n) {
    var tmp = (Math.sqrt(24 * n + 1) + 1) / 6;
    return tmp % 1 === 0;
}

var notFound = true;
var i = 143;
var result = 0;

while (notFound) {
    i++;
    result = i * (2 * i - 1);
    if (p(result)) {
        console.log(result);
        notFound = false;
    }
}